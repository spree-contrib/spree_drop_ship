<!-- insert_bottom "table#product-properties tbody" -->

<% if @product.supplier.present? %>
  <% klass = @product_properties.size % 2 == 0 ? 'even' : 'odd' %>
  <tr class="<%= klass %>">
    <td><strong>Supplier Name</strong></td>
    <td><%= @product.supplier.name %></td>
  </tr>
<% end %>
